pub struct Game {
    pub money: u32,
}
/// Restaurant Functionality

pub fn open() {
    println!("Restaurant is open.");
}

pub fn run() {
    // Create 20 Customer objects and put them in a queue
    // Only 5 customer objects can wait for an order at a time
    // If their wait time is over, they will leave the restaurant
    // If they are served, they will pay (TIP) and leave the restaurant
    // Restaurant closes after 5 minutes or if all customers are served
}

pub fn close() {
    println!("Restaurant is closed.")
}

































